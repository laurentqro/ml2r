<div class="navbar bg-base-300 text-base-content shadow-sm">
  <div class="navbar-start">
    <!-- Mobile dropdown menu -->
    <div class="dropdown">
      <div tabindex="0" role="button" class="btn btn-ghost lg:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" />
        </svg>
      </div>
      <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52">
        <li class="py-1">
          <%= link_to "Prospects", prospects_path,
              class: "#{current_page?(prospects_path) || current_page?(root_path) ? 'menu-active' : ''}",
              aria: { current: (current_page?(prospects_path) || current_page?(root_path)) ? "page" : nil } %>
        </li>
        <li class="py-1">
          <%= link_to "Clients", clients_path,
              class: "#{current_page?(clients_path) ? 'menu-active' : ''}",
              aria: { current: current_page?(clients_path) ? "page" : nil } %>
        </li>
        <li class="py-1">
          <%= link_to "Dashboard", dashboard_path,
              class: "#{current_page?(dashboard_path) ? 'menu-active' : ''}",
              aria: { current: current_page?(dashboard_path) ? "page" : nil } %>
        </li>
        <li class="py-1">
          <%= link_to "Settings", admin_risk_factor_definitions_path,
              class: "#{current_page?(admin_risk_factor_definitions_path) ? 'menu-active' : ''}",
              aria: { current: current_page?(admin_risk_factor_definitions_path) ? "page" : nil } %>
        </li>
      </ul>
    </div>
  </div>
  
  <!-- Center menu on all screens -->
  <div class="navbar-center hidden lg:flex">
    <ul class="menu menu-horizontal px-1 gap-2">
      <li>
        <%= link_to "Prospects", prospects_path,
            class: "px-4 #{current_page?(prospects_path) || current_page?(root_path) ? 'menu-active' : ''}",
            aria: { current: (current_page?(prospects_path) || current_page?(root_path)) ? "page" : nil } %>
      </li>
      <li>
        <%= link_to "Clients", clients_path,
            class: "px-4 #{current_page?(clients_path) ? 'menu-active' : ''}",
            aria: { current: current_page?(clients_path) ? "page" : nil } %>
      </li>
      <li>
        <%= link_to "Dashboard", dashboard_path,
            class: "px-4 #{current_page?(dashboard_path) ? 'menu-active' : ''}",
            aria: { current: current_page?(dashboard_path) ? "page" : nil } %>
      </li>
      <li>
        <%= link_to "Settings", admin_risk_factor_definitions_path,
            class: "px-4 #{current_page?(admin_risk_factor_definitions_path) ? 'menu-active' : ''}",
            aria: { current: current_page?(admin_risk_factor_definitions_path) ? "page" : nil } %>
      </li>
    </ul>
  </div>
  
  <div class="navbar-end">
    <!-- Optional end section content -->
  </div>
</div> 